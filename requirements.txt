# Основные зависимости
Flask==2.0.1
Flask-Login==0.5.0
aiogram==2.25.1
aiohttp==3.8.1
python-dotenv==0.19.0
gunicorn==20.1.0

# База данных
sqlite3==3.35.5  # Встроен в Python, но указан для ясности

# Дополнительные
python-jose==3.3.0  # Для JWT (если потребуется)
schedule==1.1.0     # Для планировщика задач
requests==2.26.0    # Для HTTP-запросов

# Оптимизация
uvloop==0.16.0      # Для ускорения asyncio (необязательно)
async-timeout==4.0.2 # Таймауты для асинхронных операций